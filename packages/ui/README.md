# `@cozy/ui`

Shared UI primitives for the monorepo.

These components are source-level workspace components used by app hosts such as `apps/web`.

Examples:

- `@cozy/ui/components/button`
- `@cozy/ui/components/dialog`
- `@cozy/ui/components/sonner`
