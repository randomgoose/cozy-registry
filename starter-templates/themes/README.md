# themes

Project initialization theme templates live here.

Each theme template should use the unified starter template structure:

- `template.json`
- `files/theme.css`
- optional supporting files if the theme format expands later
