# Primitives Templates

Each subdirectory in this folder maps to a `templateKey` from `lib/starter-kits.ts`.

Example:

- `primitives/button` -> `starter-templates/primitives/button`

V1 template contents are placeholders until project initialization materialization is implemented.
