# 工作日报 · 2026-03-26

## 概述

围绕 **Registry 依赖可发现性、人审确认、非阻塞健康信息** 与 **预览体验 / MCP 文案** 做了一轮落地；规范与实现已对齐。

---

## 1. MCP · `publish_component`

- 补充 **Preview props** 说明：强调可选、不传也可预览；价值在便于 **设计师在网页上看多状态**（非「预览必需」）。
- 补充 **`registryDependencies`** 说明：须 **显式声明**；建议配合 `list_components` 与只读 **`suggest_registry_dependencies`**；成功响应可带 **dependency health**（信息性、不阻塞发布）。
- 更新 `registryDependencies` 字段的 `describe`（显式、继承语义）。

---

## 2. UI · 组件卡展开预览

- 展开态 **Preview** 的 `PreviewFrame`：`fitMode` 从 `contain` 改为 **`actual`**，避免整 iframe 被 CSS `scale` 缩小导致 **视觉上「被压扁」**；与列表缩略图、详情页预览一致。

---

## 3. 规范 · Registry Dependency Management Spec

- 新增 **§1.1** 核心原则：显式依赖、可建议不可自动拍板、发布确定性、运行时解析不依赖 provenance/stub 替代声明。
- 新增 **§3.6** 依赖建议（AST + catalog、结构化 `DependencySuggestion`、与提取流水线对齐）。
- 新增 **§3.7** 发布时依赖健康（非阻塞、不自动升级）。
- 新增 **§14** Agent 一句话准则。
- §3.6.1 注明本仓库 MCP 实现入口（`suggest_registry_dependencies` + `lib/registry-dependency-suggestions.ts`）。

---

## 4. 实现 · 依赖建议与健康（MCP）

| 内容 | 说明 |
|------|------|
| **`suggest_registry_dependencies`** | 只读：对 `files` 做静态 import + 可选 stub 路径匹配，与 catalog 比对，返回 `suggestions` + `catalogSize`；不写库。 |
| **`lib/registry-dependency-suggestions.ts`** | 建议逻辑 + `toRegistryCatalogEntries`。 |
| **`lib/registry-dependency-health.ts`** | `computeRegistryDependencyHealth`、`formatDependencyHealthForMcp`；发布成功且声明了 registry 依赖时追加提示。 |
| **`lib/validate-tsx.ts`** | 导出 **`isCodeFile`**、**`resolveRelativeImport`** 供建议逻辑复用。 |
| **测试** | `registry-dependency-suggestions.test.ts`、`registry-dependency-health.test.ts`（含 `compareSemver`）。 |

---

## 5. Git 提交（供对账）

- `docs(mcp): clarify publish_component previewProps for designer review`
- `fix(ui): use 1:1 preview scale in expanded card (no CSS shrink)`
- `feat(mcp): suggest registry deps, publish health, spec §1.1/§3.6/§3.7`（含规范与讨论队列指针更新）

---

## 6. 未做 / 低优先级（备忘）

- **CSS Modules（`.module.css` default import）**：预览构建仍非完整 CSS Modules；已记为低优先级。
- **REST 与 MCP 能力对齐**（suggest / health）：可后续再开。
- **Web 勾选表单**：工作流可通过 **MCP + 对话确认** 先跑通；独立站点 UI 按需。
- **stub / de-vendor 两个已知 follow-up 风险**：
  - 该风险记录基于旧 `_deps` 安装方案；当前 V1 已切到平铺安装，后续版本隔离问题主要体现在 lockfile / direct dependency 管理，而不是目录嵌套。
  - `suggest_registry_dependencies` 对相对导入文件名命中 catalog 的建议偏激进，后续宜收敛误报/置信度。

---

## 7. 后续可执行项（简）

1. REST 侧可选暴露 suggest / 发布响应中的 health，与 MCP 对齐。
2. 在 MCP 的 suggest 返回中增加 **可复制 `registryDependencies` 片段**，降低手工拼装成本。
3. 将 suggest / health 纳入 **验收测试矩阵**（`registry-dependency-test-plan` / spec §9）。
